Run after implementing a new feature, alongside /interview-prep.

For the feature just implemented, answer:
1. What assumption about the user/adopter does this feature make (skill level, scale, use case)?
2. Does this compete with or duplicate what Sendy/Listmonk/Mautic already solve, or does it fill a real gap?
3. Is this "opinionated core" (stays as-is) or "tailorable surface" (should be documented as an extension point for teaching/customization)?
4. If you were teaching someone this decision, what would you need to explain that isn't in the ADR?
5. Would someone get the equivalent output from Klaviyo + Claude/MCP (or a comparable platform + AI agent combo) in one prompt? If yes, what does this feature offer beyond production speed — control, transparency, portability, teachability? If the answer is "nothing," flag the feature as competing on the wrong axis.

Reference specific files and functions. Do not suggest fixes — only surface and classify.

Append output to `docs/business-interview.md`, dated and titled by feature.
