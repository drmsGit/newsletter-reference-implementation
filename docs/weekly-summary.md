---
type: weekly-summary
topic:
  - project-management
  - digest
created: 2026-08-13
modified: 2026-08-13
---

# Weekly Summary

_Rolling digest, most recent week first. Generated by `/weekly-summary`. Points to the logged entries rather than restating them._

## Week of 2026-08-13

_Window: 2026-08-06 → 2026-08-13. Eight commits, all landed 2026-08-07; the omni-channel work of 2026-08-12 is still uncommitted on `main`._

### Technical

**Security base — built, reviewed, patched (5 commits, ~2,400 lines).** The whole slice landed and was immediately hardened:

- `101cd5b` — the base itself: `app/auth/` (`db_models.py`, `service.py`, `router.py`, `dependencies.py`, `permissions.py`), passwordless code sign-in, revocable sessions, `(user × role × brand)` assignment, the users list, `SYSTEM_MAIL_*` config in `.env.example`.
- `d54af00` — [policy.py](backend/app/auth/policy.py) as **one route→permission table instead of 57 decorators**, matched on the FastAPI route template. Its real property is that an unclassified write returns `UNMAPPED` and is *refused* — forgetting a route fails closed. Role administration UI (`roles.html`) plus [test_auth_policy.py](backend/tests/test_auth_policy.py), which asserts every UI write route has an entry.
- `d68fe98` / `022236e` — the two follow-up fixes found by using it: users stranded after sign-in, sign-out unavailable to some roles, `forbidden.html`, and the navigation hidden from anonymous visitors.

**ADR touched: [ADR-101](docs/architecture/ADR/ADR-101%20—%20Provider%20Capabilities%20Are%20Explicit.md)** (`d3b08da`) — dated addendum 2026-08-02, prompted by the SMTP question. Capabilities describe the *configured provider instance*, not the protocol; combined with ADR-106 they **gate where a provider may be selected**. Records the gap honestly: nothing enforces it yet, the selector still offers every registered name. No new ADRs written this week.

**Backlog — the 2026-08-07 external code review (`CODE_REVIEW_2026-08-07`), every finding verified against the code before logging.** Seven new bugs, nothing closed:

- **P0, compliance** — a frozen audience is consent-gated at plan time and never again, and the decision layer's consent guard is swallowed by a bare `except ValueError` (`delivery/service.py:383-387`). Logged with its root cause, since the broad catch is what let a compliance defect read as a rendering behaviour.
- **P1** — brand-scoped roles modelled but unioned across brands (`auth/dependencies.py:74,111`, introduced with the access model); parent send status always `sent` (`delivery/service.py:433`); webhook signature verification fails open with no secret (`adapters/resend.py:65-72`).
- **P2** — click attribution guesses one "primary" content record (`providers/service.py:100-139`), silently corrupting affinity learning; non-atomic snapshot persistence; three one-liners (`httpx2` in requirements, hard-coded `DATABASE_URL`, missing `Secure` on the session cookie).
- Features: fail-closed auth enforcement + CSRF + login-code rate limiting; test/CI gaps; **inbound machine authentication raised to P0** — it now blocks public exposure, not just Mode B.

**Uncommitted, 2026-08-12** — the omni-channel item in [backlog.md](docs/backlog.md) § Needs ADR, the design-interview write-up, and a "Before any code change" checklist added to [claude.md](docs/claude.md) (state files, intent, governing ADR, and what could break — for every edit, not only reviews). Tooling: `.claude/settings.json` now defaults to plan mode, and `/weekly-summary` was added as a command.

### Business / Project

**Playbook Decision Log gained six entries.**

- **§12–14 (dated 2026-08-02, committed `9863e0c`)** — forced login with the code on screen; **system mail as a third outbound stream** (marketing / transactional / system-internal, separated by its *circular* failure mode); and the direction that it wants a separate *transport*, not just a separate domain. Same commit refreshed the roadmap status table; `f6be25a` logged **passkeys on hold** at the user's request — adequate alternative exists, and `webauthn` brings seven transitive dependencies onto a nine-line `requirements.txt`.
- **§15–20 (2026-08-12, uncommitted)** — the **omni-channel direction**. The product is content management and orchestration for any API-reachable channel; per-channel variants under a shared campaign, coverage warned about but never enforced; organic/broadcast social **out of scope**, personalized paid social in — which *simplifies* the model, since broadcast would have forced `DeliveryExecutionDB.recipient_id` nullable. Three execution shapes survive: addressed, audience-delegated, broadcast (struck).

Two things worth carrying forward from that entry: **~80% of the architecture is already channel-neutral** as a consequence of ADR-012/022/100/101 — a checkable instance of ADR discipline paying off, better than any abstract argument for it. And the honest limit: one-to-one holds for email, push, SMS and letter but **not** paid social (minimum audience sizes, one-way hashed identifiers, aggregate feedback that must never be attributed to individuals).

**Still open, worth surfacing:**

- **Omni-channel interview deferred by the user (2026-08-12)** — five clusters / ~25 questions, expected to produce roughly a 160-block of ADRs, no code changes until it runs. Its own sequencing argument cuts against the deferral: it overlaps the **open P0 consent bug** (per-channel consent restructures the same send-time gate), and the React frontend hasn't started, so doing it later means retrofitting.
- **Auth enforcement flag: built, not switched on** — ships fail-open by default. Fine for local use, not for anything public.
- **P0 consent bug and P0 machine authentication both open** — the two current blockers on public exposure.
- No `/interview-review` or `/business-review` sessions ran this window; `business-interview.md` is unchanged since 2026-07-12 and carries no `🟡 deferred` / `⚠️ unsure` findings.
