{
  "label": "Notification",
  "channel": "push",
  "description": "The whole push message. APNs, FCM and Web Push all take structured fields inside roughly 4 KB, and the receiving OS does the rendering — so this module has no template.",
  "cms": true,
  "has_template": false,
  "variables": [
    { "name": "push_title", "required": true },
    { "name": "push_body", "required": true },
    { "name": "push_image_url", "required": false },
    { "name": "push_link", "required": false }
  ]
}
